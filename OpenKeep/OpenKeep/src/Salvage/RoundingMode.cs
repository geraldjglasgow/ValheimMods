namespace OpenKeep.Salvage
{
    /// <summary>How the fractional return of one material becomes a whole number (setting "Rounding").</summary>
    public enum RoundingMode
    {
        Floor,
        Round,
        Ceil,
    }
}
